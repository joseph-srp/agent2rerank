# GPU reranker environment for Qwen3-Reranker-0.6B inference.
# Keep this separate from requirements.txt, which is the CPU Pyserini/BM25 env.
#
# In small Docker/root filesystems, install with cache/temp on the data disk:
#   mkdir -p /root/autodl-tmp/pip-cache /root/autodl-tmp/pip-tmp
#   TMPDIR=/root/autodl-tmp/pip-tmp PIP_CACHE_DIR=/root/autodl-tmp/pip-cache pip install -r requirements-reranker-gpu.txt
#
# Tested on Python 3.10 with torch 2.5.1+cu124 and NVIDIA RTX 4090 D.

torch==2.5.1
transformers==4.57.6
accelerate==1.13.0
safetensors==0.7.0
PyYAML==6.0.3
pytest>=8,<10
